name: gcp-vm
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
description: Create a virtual machine in Google Cloud
config:
  pulumi:tags:
    value:
      pulumi:template: gcp-python
